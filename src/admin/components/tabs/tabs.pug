.tabs-wrapper
  .container-tabs
    ul.tabs
      li.tabs__item(v-for="tab in tabs")
        router-link(
          :data-text="tab.title" 
          :to="tab.href"
          exact-active-class="active"
        ).tabs__link